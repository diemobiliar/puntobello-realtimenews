name: azd-starter
resourceGroup: ${rg}
infra:
  provider: terraform
services:
  app:
    resourceName: ${app_name}
    project: ./puntobello-realtimenews-webapp
    host: appservice
    language: js
hooks:
  preup:
    shell: pwsh
    run: ./hooks/preup.ps1
  postup:
    shell: pwsh
    run: ./hooks/postup.ps1